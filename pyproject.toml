[project]
name = "ieee-cis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rhys", email = "rhys3680@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[project.scripts]
ieee-cis = "ieee_cis:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "kaggle>=2.2.4",
]
